name: place_finder_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.3.0
  provider: ^6.1.1

  cupertino_icons: ^1.0.8
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12
  latlong2: ^0.10.1
  http: ^1.6.0
  geolocator: ^14.0.3
  flutter_map: ^8.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
